database:
  image: admin.datapunt.amsterdam.nl:5000/atlas/postgres
  environment:
    POSTGRES_PASSWORD: insecure
    POSTGRES_USER: panorama

importer:
  build: ../web
  links:
    - database:database
  volumes:
    - /mnt/pano2016:/app/panoramas/2016
  environment:
    DB_NAME: panorama
    DB_PASSWORD: insecure
    PANORAMA_DATA_DIR: /app/panoramas
  command: >
    bash -c "/app/docker-wait.sh \
            && python manage.py migrate \
            && python manage.py run_import"

db-backup:
  image: admin.datapunt.amsterdam.nl:5000/atlas/postgres
  links:
    - database:db
  volumes:
    - ./backups:/tmp/backups
  command: >
    bash -c "echo db:5432:panorama:panorama:insecure > ~/.pgpass \
            && chmod 600 ~/.pgpass \
            && pg_dump --clean \
                        -Fc \
                        -t panoramas* \
                        -t geo*  \
                        -T auth*    \
                        -U panorama \
                        -h db -p 5432 \
                        panorama > /tmp/backups/database.dump"
